const fs = require('fs'); const data = JSON.parse(fs.readFileSync('C:/Users/HP/Downloads/260914_instant_29681777a8344f32baf233b9f1f3f358.har', 'utf8')); const entries = data.log.entries; console.log('Total Requests: ' + entries.length); const largest = entries.sort((a,b) => b.response.bodySize - a.response.bodySize).slice(0, 5); console.log('\nTop 5 Largest Payloads:'); largest.forEach(e => console.log(e.request.url.substring(0,80) + ' - ' + (e.response.bodySize/1024).toFixed(2) + ' KB')); const longest = entries.sort((a,b) => b.time - a.time).slice(0, 5); console.log('\nTop 5 Longest Requests (ms):'); longest.forEach(e => console.log(e.request.url.substring(0,80) + ' - ' + e.time.toFixed(0) + ' ms'));
